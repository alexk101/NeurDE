# @package scheduler
# CosineAnnealingLR scheduler configuration (stepped per epoch)

scheduler_type: CosineAnnealingLR
# T_max set from training.epochs at runtime; override here if needed
eta_min: 5.01e-6
